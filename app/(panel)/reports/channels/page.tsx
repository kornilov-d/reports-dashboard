import TabPlaceholder from "@/components/inventory/TabPlaceholder";

export default function ChannelsReportPage() {
  return (
    <TabPlaceholder
      title="Channels report"
      description="The existing Channels report lives here. The home dashboard reads from this data source."
    />
  );
}
