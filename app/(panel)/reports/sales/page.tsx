import TabPlaceholder from "@/components/inventory/TabPlaceholder";

export default function SalesReportPage() {
  return (
    <TabPlaceholder
      title="Sales report"
      description="The existing Sales report lives here. The home dashboard reads from this data source."
    />
  );
}
