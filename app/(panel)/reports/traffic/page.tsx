import TabPlaceholder from "@/components/inventory/TabPlaceholder";

export default function TrafficReportPage() {
  return (
    <TabPlaceholder
      title="Traffic report"
      description="The existing Traffic report lives here. The home dashboard reads from this data source."
    />
  );
}
